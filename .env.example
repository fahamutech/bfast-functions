# Shared runtime defaults for bfast-function
# Copy to .env and adjust values for your environment.

PORT=3000
MODE=git

# Request payload size limit used by Express body parsers
BFAST_BODY_LIMIT=2mb

# Optional: run a custom script instead of starting the built-in HTTP server
# START_SCRIPT=node start.js

# Local mode notes:
# MODE=local is supported with these additional vars:
# FUNCTIONS_DIR_PATH=./functions
# BFAST_JSON_PATH=./bfast.json
# ASSETS_PATH=./assets (optional)
# See .env.local.example for a full local-mode template.

# Mode-specific values (set based on MODE)
# GIT_CLONE_URL=https://github.com/acme/functions.git
# GIT_USERNAME=acme-bot
# GIT_TOKEN=replace-with-token
# NPM_TAR=@acme/functions@1.0.0
# URL_TAR=https://example.com/functions.tgz
