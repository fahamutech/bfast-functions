# NPM package mode example

PORT=3000
MODE=npm
BFAST_BODY_LIMIT=2mb

# Supports package, @scope/package, or package@version
NPM_TAR=@acme/functions@1.0.0

# Optional custom script (if set, built-in server startup is skipped)
# START_SCRIPT=node start.js
